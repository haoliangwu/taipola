/**
 * The document a first-time visitor lands in, and the name it answers to.
 *
 * The two travel together on purpose: the title bar shows this name, the save
 * picker suggests it and an export is named after it. Spelling it out at each of
 * those places is how the title bar ended up saying one thing while the save
 * dialog said `untitled.md`.
 */
export const WELCOME_NAME = 'welcome.md'

export const WELCOME_DOC = `# 你好呀，我是 taipola

我是住在你浏览器里的一个极简 Markdown 编辑器。

**光标停在哪一段，我就把那一段的 Markdown 源码摊开给你看；光标一走开，我马上把它变回好看的样子。**

没有左右分栏，也没有"预览"按钮。你可以用方向键上下走走看，我就当着你的面变来变去。

## 我会些什么

- **即时渲染**：段落、标题、列表、引用、代码块、表格、行内数学，就地帮你变
- **Typora 的键位**：\`⌘B\` 加粗、\`⌃\`\` 行内代码、\`⌥⌘Q\` 引用……手不离键盘，下面有一张表
- **中文输入法友好**：不抢拼音候选，不打断联想词
- **自带撤销栈**：\`Cmd/Ctrl + Z\` / \`Cmd/Ctrl + Shift + Z\`，跨块也靠得住
- **打开 / 保存**：\`Cmd/Ctrl + O\` 打开，\`Cmd/Ctrl + S\` 保存，浏览器允许就写回原文件
- **侧边大纲**：点一下标题就跳过去
- **自动保存草稿**：手滑关掉页面也还记得你写到哪儿了

## 行内样式

**粗体**、*斜体*、~~删除线~~、\`行内代码\`、[链接](https://github.com/haoliangwu/taipola)，还有脚注[^1]，这些我都认得。

\`⌘\\\`（清除格式）可以把这些行内样式剥干净，只留下文字本身。

[^1]: 像我这样的小小补充，留在原地；导出成 HTML 时才挪到文末去。

## 行内数学

三种写法都认得：$E = mc^2$、\\(a^2 + b^2 = c^2\\)、\\[\\int_0^1 x^2 \\, dx\\]。

\`⌃M\` 切换一对 \`$\`，光标落在中间；数学默认开着，不用先找开关。

## 标题分级

\`⌘1\`–\`⌘6\` 直接设级别，\`⌘0\` 还原成普通段落，\`⌘=\` / \`⌘-\` 升一级 / 降一级。

# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题

## 无序列表

- 用 \`-\`、\`*\` 或 \`+\` 开头，在我眼里都是一项
- 光标一离开这个块，符号就缩成一个小圆点
- 缩进两个空格，就是我收下的子项

## 有序列表

1. 打开 / 保存
2. 大纲跳转
3. 主题切换

## 列表嵌套

- 第一层列表项
  - 缩进两个空格的子项
  - 另一个子项
    1. 嵌套的有序列表
    2. 继续编号
- 回到第一层

## 引用

> 写作是把思绪压进纸张的过程。—— 某个已经想不起来的人

> 嵌套引用我也照收：
> > 被引用的引用，照样好好渲染。

## 任务列表

- [x] 做一个即时渲染的编辑器
- [ ] 圆角、阴影、暗色主题
- [ ] mermaid 图表与公式块

方框不止是画出来的：**光标不在这一行的时候，直接点它就能勾上**，再点一下取消，撤销键照样管用。整行想加成 / 去掉任务，用 \`⌥⌘X\`。

## 代码高亮

\`\`\`typescript
interface Block {
  startLine: number
  raw: string
  html: string
}

export function activeBlock(blocks: Block[], caret: number): Block | undefined {
  return blocks.find((b) => caret >= b.start && caret < b.end)
}
\`\`\`

没写语言的围栏，我也照收：

\`\`\`
echo "hello, taipola"
\`\`\`

## 表格

表格里 \`Tab\` / \`⇧Tab\` 走格子，最后一格再按一次就添一行；\`⌘⏎\` / \`⇧⌘⏎\` 插行、\`⇧⌘⌫\` 删行，增删列在格子里右键。记不住的话，右上角问号里也有一份。

| 快捷键 | 作用 |
| --- | --- |
| \`⌘B\` / \`⌘I\` | 加粗 / 斜体 |
| \`\` ⌃\` \`\` / \`\` ⌃⇧\` \`\` | 行内代码 / 删除线 |
| \`⌘K\` / \`⌃M\` | 插入链接 / 行内数学 |
| \`⌘1\`–\`⌘6\` / \`⌘0\` | 标题 1–6 级 / 还原成段落 |
| \`⌘=\` / \`⌘-\` | 标题升一级 / 降一级 |
| \`⌘\\\` | 清除行内格式 |
| \`⌥⌘Q\` / \`⌥⌘U\` / \`⌥⌘O\` / \`⌥⌘X\` | 引用 / 无序列表 / 有序列表 / 任务列表 |
| \`⌘]\` / \`⌘[\` | 列表缩进 / 反缩进 |
| \`⌥⌘C\` / \`⌥⌘T\` / \`⌥⌘-\` | 代码块 / 表格 / 水平线 |
| \`⌥⌘R\` / \`⌥⌘L\` | 脚注 / 链接引用 |
| \`⌘⇧K\` / \`⌘⇧\\\` | 删除整行 / 大纲开关 |
| \`Tab\` / \`⇧Tab\` | 表格里：下一格 / 上一格（最后一格再按 \`Tab\` 加一行） |
| \`⌘⏎\` / \`⇧⌘⏎\` / \`⇧⌘⌫\` | 表格里：下方插行 / 上方插行 / 删掉本行 |
| 格子里右键 | 增删列、删除整张表 |

## 图片与链接

图片也支持：下面这张是我的自拍——黑白配，还翘着一根呆毛。

![taipola 的自拍](https://raw.githubusercontent.com/haoliangwu/taipola/main/docs/logo.webp){width=160}

随手打出来的网址也认：https://github.com/haoliangwu/taipola 就像这样，夹在句子里的 https://haoliangwu.github.io/taipola/ 也跑不掉。

那个 GitHub 网址是我的家。觉得好用的话，回家顺手给我点一颗小星星吧，就一颗。

## 分割线

三个短横线就是一条水平线，\`⌥⌘-\` 也能直接插一条：

---

好啦，接下来就交给你了。这篇可以直接删，也可以改成你自己的第一句话。
`
