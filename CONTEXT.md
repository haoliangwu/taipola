# taipola

一个极简的 Markdown 编辑器：持有光标的语法块显示它的 Markdown 源码，其余部分就地渲染。
编辑器内核是原生的、自己拥有 DOM 的；界面外壳只负责外壳。

## Language

**文档**:
应用内的编辑单位。它可能对应磁盘上的一个文件，也可能没有对应文件（欢迎文档、未命名文档）。
_Avoid_: 文件（指编辑单位时）、buffer、标签

**当前文档**:
此刻在编辑区里的那一份文档。全局只有一个：换文档就是整片换掉，不存在同时打开的多份文档。
_Avoid_: 标签页、tab、打开的多个文件

**文件夹根**:
用户在文件树里打开的那个目录。它给文件树一个范围，全局最多一个，再打开一个就替换上一个。
_Avoid_: 工作区、workspace、项目、仓库

**文件树**:
文件夹根之下的层级视图，只呈现 Markdown 文档与子目录。它是「还能写哪几篇」的回答。
_Avoid_: 文件列表、目录树、资源管理器

**写回**:
把文档的内容送进它所对应的文件。没有对应文件的文档不存在写回。
_Avoid_: 保存（保存是用户的动作，写回是内容的去向）、同步、上传

**草稿**:
尚未写回的内容在本机留下的副本，用来让刷新、崩溃与意外关闭不至于带走正在写的东西。
它按文档分开存放，写回成功之后就不再需要。
_Avoid_: 缓存、备份、自动保存的文件

**行内标记**:
挂在文字两侧的排版记号（`**` 粗体、`*` 斜体、`~~` 删除线、`` ` `` 代码、链接），
在同一行内成对出现、可嵌套。它是行内格式命令（加粗、清除格式）作用的对象。
区别于「行内数学」：行内数学是内容表达式而非排版记号。
_Avoid_: 行内样式、inline style、格式符号

**行内数学**:
一对定界符之间的一段数学表达式，是嵌入正文的内容而非排版记号：⌃M 插入/切换、
渲染为独立算式。定界符三种形态: `$…$`、`\(…\)`、`\[…\]`（均 Pandoc 标准；
`\(…\)`/`\[…\]` 即 Typora 所称 legacy LaTeX 定界符，默认解析）。清除格式会
连定界符一起剥掉、表达式文字保留（与剥 `**` 同类）。
_Avoid_: 公式、LaTeX、math、行内标记（指数学时）

**块级前缀**:
一行的开头、决定整行块身份的记号（`#` 标题、`> ` 引用、`- `/`1. ` 列表、`- [ ] `
任务）。它跟行内标记的区别是只出现在行首、不成对。
_Avoid_: 块级标记、block marker、行首符号

**块级切换**:
对光标所在行（或选区覆盖的每一行）加上或剥掉某一种块级前缀的命令——引用、无序列表、
有序列表、任务列表四者都是同一种「加剥前缀」的形态。区别于只做插入的片段命令。
_Avoid_: toggle、格式化、切换格式
