node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.tsbuildinfo
.dsh-vision-toolkit
.pw-browsers

# The issue tracker (see docs/agents/issue-tracker.md). Local working state for
# this checkout, deliberately absent from the repository's history.
.scratch/
